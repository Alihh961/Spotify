<div class="row">
	{{ form_start(form ,{"attr": {
    'class': 'm-auto col-6 text-center'
}}) }}

	<div
		class="d-flex gap-3 justify-content-center my-3 con">

		{# {{ form_label(form.firstName ,{"attr": {
		    'class': 'm-auto col-6 text-center'
		}} )}} #}
		{{ form_label(form.firstName)}}

		{{ form_widget(form.firstName)}}


	</div>
	<div class="d-flex gap-3 justify-content-center my-3">

		{{ form_label(form.lastName)}}
		{{ form_widget(form.lastName)}}

	</div>

	<div class="d-flex gap-3 justify-content-center my-3">

		{{ form_label(form.music)}}
		{{ form_widget(form.music)}}

	</div>

	<button class="btn btn-success my-3">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</div>
